============
Installation
============

At the command line::

    $ easy_install django-recaptcha-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-recaptcha-utils
    $ pip install django-recaptcha-utils
